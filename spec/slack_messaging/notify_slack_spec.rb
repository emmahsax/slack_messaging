require "slack_messaging"

module SlackMessaging
  describe NotifySlack do
    
    it "should ping Slack" do
        # expect(Aws::SharedCredentials).to receive(:new).with(profile_name: 'staging')
        # AwsAuditor::NotifySlack.new("test string")
    end
    
  end
end
